kind: ConfigMap
apiVersion: v1
metadata:
  name: eap-config
data: 
  # Configuration to connnect to PostgreSQL
  POSTGRESQL_DATABASE: frdemo
  POSTGRESQL_HOST: postgresql
  POSTGRESQL_JNDI: java:/FRDemoDS
  KAFKA_SERVER: frdemo-kafka-bootstrap:9092
